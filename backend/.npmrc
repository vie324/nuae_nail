# Skip the automatic Chromium download on `npm install`.
# Run `npm run browsers:install` (or `npx playwright install chromium`) once
# manually. Keeps `npm install` fast and CI-friendly.
playwright_skip_browser_download=1
